---
title: 用户确认键位 2026-08-09
project: 个人 Unicode 音型输入法
type: user-decision
date: 2026-08-09
---

# 用户确认键位 2026-08-09

本页只记录本次明确确认的规则与键位，用作实体页的决策来源，不替代冷归档原始资料。

## 新确认条目

| 字符 | 输入码 | 说明 |
| --- | --- | --- |
| ༳ | `my` | `my` 二码候选；同时作为“茂ܜ”来源组的默认前缀。 |
| ܜ | `mcck` | `mc**` 前缀；曲线＋带钩。历史码 `mymc` 保留。 |
| ¤ | `qtrs` | `qt**` 前缀；星芒＋对称。历史码 `qtqy` 保留。 |

## 规则优先级

1. 用户明确指定的完整输入码最高优先，既有码不因批量规则改写。
2. `jj` 单独保留给 `𓆟`，但允许继续使用 `jj??` 四码。
3. 最新指定的两键前缀覆盖旧的来源组推断；旧四码只作为历史记录。
4. 第三键使用主骨架，第四键使用状态；同形态允许重码候选。

## 2026-08-09 第二次调整

| 字符  | 当前码    | 历史码    |
| --- | ------ | ------ |
| ଉ   | `boki` | `boki` |
| Պ   | `ascg` | `asmm` |
| ○   | `yboe` | `ybin` |
| ◌   | `axde` | `axth` |
| ⦿   | `ddoi` | `ddda` |
| ●   | `fpof` | `fpys` |
| 𒁔  | `mvma` | `mvsu` |
| ᗚ   | `qdcg` | `qdvb` |
| ஆ   | `pici` | `paii` |
| ᗣ   | `lnte` | `limn` |

`𓆟字块.md` 中 U+13000..U+1342F 的 1,072 个埃及象形文字分为四个前缀，并由项目生成器按形态初分；每个四码最多5个候选，`𓆟 = jj` 二码继续保留。

| 前缀 | 范围 | 数量 |
| --- | --- | ---: |
| `jj` | 生命形，包含人物、人体、动物、鸟、鱼和 `𓆟` | 429 |
| `jn` | 自然、天地、水和植物 | 163 |
| `jw` | 器物、建筑和工具 | 274 |
| `jx` | 图形、抽象与未分类 | 206 |

`𓆟` 的人工形态四码为 `jjgh`：动物有机体＋横向。

## 关联冷归档

- [[90_旧库冷归档/Unicode原库_迁移前快照/茂ܜ/符号/自创语/༳/༳]]
- [[90_旧库冷归档/Unicode原库_迁移前快照/茂ܜ/符号/自创语/𓆟/¤]]
- `ܜ` 是“茂ܜ”命名与本次用户确认中的字符；限定候选目录中没有找到以 `ܜ` 为标题或正文命中的独立来源笔记，因此不伪造冷归档路径。
